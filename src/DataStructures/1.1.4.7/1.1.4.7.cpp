// 1.1.4.7ќтветьте на вопрос: как в исходном коде отличить операцию разыменовани€, 
//объ€вление указател€ и операцию умножени€ ? ¬се три операции используют символ*,
//поэтому важно уметь отличать их в исходном коде.

//ѕри умножении используютс€ две переменные и * расположена между ними.
//ѕри объ€влении указател€ знак * используетс€ после типа данных перед наименованием переменной.
//ѕри разыменовании тип переменной не указываетс€, а также используетс€ лишь одна переменна€. 